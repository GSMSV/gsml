# ─── Core ───────────────────────────────────────────────
APP_TIMEZONE=Asia/Seoul
CORS_ORIGINS=http://localhost:5173

# ─── Upstream LLM (llama-server or vLLM) ───────────────
UPSTREAM_BASE_URL=http://host.docker.internal:8080
UPSTREAM_TIMEOUT=600
# /v1/chat/completions에 id_slot을 주입해 슬롯을 고정(KV 캐시 재사용)할지 여부.
# 업스트림 빌드가 OAI 엔드포인트의 id_slot 패스스루를 지원하지 않으면 false.
# (도구 호출에는 llama-server 측 --jinja + 도구 지원 템플릿 필요 — upstream.yml 주석 참고)
UPSTREAM_SLOT_PINNING=true

# ─── DataGSM OAuth ─────────────────────────────────────
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URI=http://localhost:5173/auth/callback
OAUTH_AUTH_BASE=https://oauth.authorization.datagsm.kr
OAUTH_RESOURCE_BASE=https://oauth.resource.datagsm.kr

# ─── JWT ───────────────────────────────────────────────
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRE_HOURS=24

# ─── API Key ───────────────────────────────────────────
API_KEY_EXPIRE_DAYS=30

# ─── Defaults for new users ────────────────────────────
DEFAULT_USAGE_LIMIT=100000
DEFAULT_MAX_CONCURRENT=2

# ─── Logs ──────────────────────────────────────────────
REQUEST_LOG_RETENTION_DAYS=30
CONVERSATION_LOG_DIR=/app/logs/conversations

# ─── Database ──────────────────────────────────────────
DATABASE_URL=sqlite:///./data/gsml.db
